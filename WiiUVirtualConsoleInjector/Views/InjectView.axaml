<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="using:WiiUVirtualConsoleInjector.ViewModels"
             xmlns:vc="using:PD.WiiU.VirtualConsole"
             xmlns:wiiu="using:WiiUSharp"
             xmlns:conv="using:WiiUVirtualConsoleInjector.Converters"
             xmlns:behaviors="using:WiiUVirtualConsoleInjector.Behaviors"
             x:Class="WiiUVirtualConsoleInjector.Views.InjectView"
             x:DataType="vm:InjectViewModel">

    <UserControl.Styles>
        <!-- Inline link styled as text -->
        <Style Selector="Button.link">
            <Setter Property="Padding" Value="0" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="FontSize" Value="12" />
            <Setter Property="FontWeight" Value="Normal" />
            <Setter Property="Foreground" Value="{DynamicResource CyanBrush}" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="Template">
                <ControlTemplate>
                    <ContentPresenter Name="PART_ContentPresenter" Content="{TemplateBinding Content}" Foreground="{TemplateBinding Foreground}" />
                </ControlTemplate>
            </Setter>
        </Style>
        <Style Selector="Button.link TextBlock">
            <Setter Property="TextDecorations" Value="Underline" />
        </Style>
        <Style Selector="Button.link:pointerover">
            <Setter Property="Foreground" Value="{DynamicResource CyanDarkBrush}" />
        </Style>

        <!-- Clickable artwork slot -->
        <Style Selector="Button.slot">
            <Setter Property="Padding" Value="0" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <ControlTemplate>
                    <ContentPresenter Name="PART_ContentPresenter" Content="{TemplateBinding Content}" />
                </ControlTemplate>
            </Setter>
        </Style>
        <Style Selector="Button.slot:pointerover Rectangle.slot-frame">
            <Setter Property="Stroke" Value="{DynamicResource CyanFocusBrush}" />
        </Style>
        <Style Selector="Rectangle.slot-frame">
            <Setter Property="Fill" Value="{DynamicResource SubtleBrush}" />
            <Setter Property="Stroke" Value="{DynamicResource InputBorderBrush}" />
            <Setter Property="StrokeThickness" Value="1.5" />
            <Setter Property="StrokeDashArray" Value="4,3" />
        </Style>
        <Style Selector="Button.tiny">
            <Setter Property="FontSize" Value="11" />
            <Setter Property="Padding" Value="10,3" />
        </Style>

        <!-- Stroke glyphs in the base list -->
        <Style Selector="Path.glyph">
            <Setter Property="StrokeThickness" Value="2" />
            <Setter Property="StrokeLineCap" Value="Round" />
            <Setter Property="StrokeJoin" Value="Round" />
        </Style>

        <!-- Review summary -->
        <Style Selector="TextBlock.summary-label">
            <Setter Property="FontSize" Value="12.5" />
            <Setter Property="FontWeight" Value="Bold" />
            <Setter Property="Foreground" Value="{DynamicResource TextSecondaryBrush}" />
            <Setter Property="Width" Value="90" />
        </Style>
        <Style Selector="TextBlock.summary-value">
            <Setter Property="FontSize" Value="13" />
            <Setter Property="TextWrapping" Value="Wrap" />
        </Style>
    </UserControl.Styles>

    <Grid>
        <!-- Wizard column -->
        <StackPanel Spacing="14" MaxWidth="900" Margin="80,0" HorizontalAlignment="Stretch">
            <!-- Step dots, with Start over off to the side -->
            <Grid>
                <StackPanel Spacing="8" HorizontalAlignment="Center">
                    <ListBox Classes="dots" ItemsSource="{x:Static vm:InjectViewModel.Steps}" SelectedItem="{Binding SelectedStep}" HorizontalAlignment="Center" Padding="0">
                        <ListBox.ItemTemplate>
                            <DataTemplate x:DataType="vm:WizardStep">
                                <Border Classes="dot" ToolTip.Tip="{Binding Label}" />
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                    <TextBlock Text="{Binding CurrentWizardStep.Label}" FontSize="11" FontWeight="Bold" Foreground="{DynamicResource TextTertiaryBrush}" LetterSpacing="0.2" HorizontalAlignment="Center" />
                </StackPanel>
                <Button Classes="link" Command="{Binding StartOverCommand}" HorizontalAlignment="Right" VerticalAlignment="Center" ToolTip.Tip="Clear every field and go back to the first step">
                    <TextBlock Text="Start over" />
                </Button>
            </Grid>

            <Border Classes="card">
                <Panel>
                    <!-- 1. Console -->
                    <StackPanel IsVisible="{Binding IsConsoleStep}">
                        <Grid ColumnDefinitions="*,Auto">
                            <TextBlock Grid.Column="0" Classes="section" Text="Choose the source console" IsVisible="{Binding !IsGroupOpen}" />
                            <TextBlock Grid.Column="0" Classes="section" Text="{Binding OpenGroupName, StringFormat='{}{0} consoles'}" IsVisible="{Binding IsGroupOpen}" />
                            <Button Grid.Column="1" Classes="link" Command="{Binding CloseGroupCommand}" IsVisible="{Binding IsGroupOpen}" VerticalAlignment="Top" Margin="0,4,0,0">
                                <TextBlock Text="‹ All consoles" />
                            </Button>
                        </Grid>
                        <!-- two grids, five 152px tiles per row; the top one only hides while a company is open, so the tile that took the click keeps the pointer -->
                        <ListBox Classes="tiles" ItemsSource="{Binding TopTiles}" SelectedItem="{Binding SelectedTopTile}" IsEnabled="{Binding !IsRunning}" IsVisible="{Binding !IsGroupOpen}"
                                 MaxWidth="760" Padding="0" HorizontalAlignment="Center">
                            <ListBox.ItemTemplate>
                                <DataTemplate x:DataType="vm:ConsoleTile">
                                    <StackPanel>
                                        <Border Classes="tile-frame">
                                            <Panel>
                                                <Border Classes="tile-panel">
                                                    <Image Classes="tile-logo" Source="{Binding, Converter={x:Static conv:ConsoleConverters.TileDarkIcon}}" Opacity="0.8" />
                                                </Border>
                                                <Border Classes="tile-gloss" />
                                            </Panel>
                                        </Border>
                                        <TextBlock Classes="tile-label" Text="{Binding Label}" />
                                        <TextBlock Classes="tile-caption" Text="{Binding Caption}" IsVisible="{Binding Caption, Converter={x:Static StringConverters.IsNotNullOrEmpty}}" />
                                    </StackPanel>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                        <ListBox Classes="tiles" ItemsSource="{Binding Tiles}" SelectedItem="{Binding SelectedTile}" IsEnabled="{Binding !IsRunning}" IsVisible="{Binding IsGroupOpen}"
                                 MaxWidth="760" Padding="0" HorizontalAlignment="Center">
                            <ListBox.ItemTemplate>
                                <DataTemplate x:DataType="vm:ConsoleTile">
                                    <StackPanel>
                                        <Border Classes="tile-frame">
                                            <Panel>
                                                <Border Classes="tile-panel">
                                                    <Image Classes="tile-logo" Source="{Binding, Converter={x:Static conv:ConsoleConverters.TileDarkIcon}}" Opacity="0.8" />
                                                </Border>
                                                <Border Classes="tile-gloss" />
                                            </Panel>
                                        </Border>
                                        <TextBlock Classes="tile-label" Text="{Binding Label}" />
                                        <TextBlock Classes="tile-caption" Text="{Binding Caption}" IsVisible="{Binding Caption, Converter={x:Static StringConverters.IsNotNullOrEmpty}}" />
                                    </StackPanel>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                    </StackPanel>

                    <!-- 2. Game: base, then the ROM and its names -->
                    <StackPanel IsVisible="{Binding IsGameStep}">
                        <TextBlock Classes="section" Text="{Binding SelectedConsoleName, StringFormat='{}{0} game'}" Margin="0,0,0,4" />
                        <StackPanel IsVisible="{Binding IsRetroArch}">
                            <TextBlock Classes="label" Text="RetroArch core" Margin="0,8,0,2" />
                            <TextBlock Classes="hint" FontSize="12" Text="The core is the emulator; it is packed into the title with your ROM. The title runs under Aroma only and uses the retroarch folder on the SD card for settings and saves." Margin="0,0,0,14" />
                            <ItemsControl ItemsSource="{Binding BiosFiles}" Margin="0,0,0,14">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate x:DataType="vm:BiosFileViewModel">
                                        <StackPanel Spacing="4" Width="560" HorizontalAlignment="Left" Margin="0,0,0,10">
                                            <ContentControl Content="{Binding Field}" />
                                            <StackPanel Orientation="Horizontal" Spacing="8" IsVisible="{Binding HasChoice}">
                                                <TextBlock Classes="label" Text="Name on the card" VerticalAlignment="Center" />
                                                <ComboBox ItemsSource="{Binding Names}" SelectedItem="{Binding SelectedName}" Width="240" IsEnabled="{Binding !IsOnCard}" />
                                            </StackPanel>
                                            <TextBlock Classes="hint" FontSize="12" Text="{Binding Status}" TextWrapping="Wrap" />
                                        </StackPanel>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                            <ComboBox ItemsSource="{Binding Cores}" SelectedItem="{Binding SelectedCore}" Width="480" HorizontalAlignment="Left" IsEnabled="{Binding !IsRunning}" PlaceholderText="Select a core…">
                                <ComboBox.ItemTemplate>
                                    <DataTemplate x:DataType="vc:RetroArchCore">
                                        <StackPanel VerticalAlignment="Center">
                                            <TextBlock Text="{Binding Name}" FontSize="13" FontWeight="Bold" TextTrimming="CharacterEllipsis" />
                                            <TextBlock Text="{Binding Description}" FontSize="10.5" Foreground="{DynamicResource TextTertiaryBrush}" TextTrimming="CharacterEllipsis" />
                                        </StackPanel>
                                    </DataTemplate>
                                </ComboBox.ItemTemplate>
                            </ComboBox>
                        </StackPanel>
                        <StackPanel IsVisible="{Binding !IsRetroArch}">
                            <TextBlock Classes="label" Text="Base" Margin="0,8,0,2" />
                            <WrapPanel ItemSpacing="6" Margin="0,0,0,14">
                                <TextBlock Classes="hint" FontSize="12" Text="The base holds the Virtual Console emulator; your ROM gets patched into it. Which games work on which base is community-tested:" VerticalAlignment="Center" />
                                <Button Classes="link" Command="{Binding OpenCompatibilityListCommand}" ToolTip.Tip="Opens the UWUVCI compatibility page for this console in your browser">
                                    <TextBlock Text="Compatibility list ↗" />
                                </Button>
                            </WrapPanel>
                            <ComboBox ItemsSource="{Binding Bases}" SelectedItem="{Binding SelectedBase}" Width="480" HorizontalAlignment="Left" IsEnabled="{Binding !IsRunning}" PlaceholderText="Select a base…">
                                <ComboBox.ItemContainerTheme>
                                    <ControlTheme TargetType="ComboBoxItem" BasedOn="{StaticResource {x:Type ComboBoxItem}}" x:DataType="vm:BaseChoice">
                                        <Setter Property="Opacity" Value="{Binding IsUsable, Converter={x:Static conv:StatusConverters.UsableOpacity}}" />
                                    </ControlTheme>
                                </ComboBox.ItemContainerTheme>
                                <ComboBox.ItemTemplate>
                                    <DataTemplate x:DataType="vm:BaseChoice">
                                        <Grid ColumnDefinitions="Auto,*,Auto,Auto,Auto" ColumnSpacing="10">
                                            <Border Grid.Column="0" Width="22" Height="15" CornerRadius="2" BorderThickness="1" BorderBrush="#26000000" VerticalAlignment="Center"
                                                    Background="{Binding Base.Region, Converter={x:Static conv:RegionConverters.Flag}}" />
                                            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                                <TextBlock Text="{Binding Base.Name}" FontSize="13" FontWeight="Bold" TextTrimming="CharacterEllipsis" />
                                                <TextBlock Classes="mono" Text="{Binding TitleId}" FontSize="10.5" Foreground="{DynamicResource TextTertiaryBrush}" />
                                            </StackPanel>
                                            <Border Grid.Column="2" Classes="tag" IsVisible="{Binding Base.IsRecommended}" VerticalAlignment="Center" ToolTip.Tip="The base the community found works best for this console">
                                                <TextBlock Text="Recommended" FontSize="10.5" />
                                            </Border>
                                            <Viewbox Grid.Column="3" Width="16" Height="16" VerticalAlignment="Center" ToolTip.Tip="Title key added">
                                                <Canvas Width="24" Height="24">
                                                    <Path Classes="glyph" Stroke="{Binding HasTitleKey, Converter={x:Static conv:StatusConverters.Glyph}}"
                                                          Data="M12 8 A4 4 0 1 1 4 8 A4 4 0 1 1 12 8 M10.8 10.8L20 20 M15.5 15.5l2.2 -2.2 M18 18l2.2 -2.2" />
                                                </Canvas>
                                            </Viewbox>
                                            <Viewbox Grid.Column="4" Width="16" Height="16" VerticalAlignment="Center" ToolTip.Tip="Downloaded">
                                                <Canvas Width="24" Height="24">
                                                    <Path Classes="glyph" Stroke="{Binding IsPresent, Converter={x:Static conv:StatusConverters.Glyph}}"
                                                          Data="M12 3v11 M7.5 10.5L12 15l4.5-4.5 M4 17.5V19a1 1 0 0 0 1 1h14a1 1 0 0 0 1-1v-1.5" />
                                                </Canvas>
                                            </Viewbox>
                                        </Grid>
                                    </DataTemplate>
                                </ComboBox.ItemTemplate>
                            </ComboBox>
                            <WrapPanel ItemSpacing="6" Margin="0,12,0,0" IsVisible="{Binding BaseHint, Converter={x:Static StringConverters.IsNotNullOrEmpty}}">
                                <TextBlock Classes="warning" Text="{Binding BaseHint}" VerticalAlignment="Center" />
                                <Button Classes="link" Command="{Binding ManageBasesCommand}">
                                    <TextBlock Text="Manage bases &amp; keys →" />
                                </Button>
                            </WrapPanel>
                        </StackPanel>
                        <Border Classes="divider" Margin="0,18,0,18" />
                        <StackPanel Spacing="16" Width="560" HorizontalAlignment="Left">
                            <StackPanel Spacing="4">
                                <TextBlock Classes="label" Text="ROM" />
                                <Grid ColumnDefinitions="*,Auto,Auto,Auto" ColumnSpacing="10">
                                    <TextBox Grid.Column="0" Text="{Binding RomPath}" IsReadOnly="True" PlaceholderText="Required" />
                                    <Button Grid.Column="1" Classes="sm" Content="Pick ROM" Command="{Binding PickRomCommand}" IsEnabled="{Binding !IsRunning}" />
                                    <Button Grid.Column="2" Classes="sm" Content="Pick TurboCD folder" Command="{Binding PickTurboCdFolderCommand}" IsVisible="{Binding IsTurboCd}" IsEnabled="{Binding !IsRunning}" />
                                    <Button Grid.Column="3" Classes="ghost sm" Content="Clear" Command="{Binding ClearRomCommand}" IsVisible="{Binding HasRom}" IsEnabled="{Binding !IsRunning}" />
                                </Grid>
                                <TextBlock Classes="hint" Text="{Binding RomExtensions, StringFormat='Accepts {0}'}" Margin="0,1,0,0" />
                                <Border Classes="banner-warning" Margin="0,8,0,0" IsVisible="{Binding HasRomFitHint}">
                                    <StackPanel Spacing="6">
                                        <TextBlock Text="{Binding RomFitHint}" />
                                    </StackPanel>
                                </Border>
                            </StackPanel>
                            <StackPanel Spacing="4">
                                <TextBlock Classes="label" Text="Long name" />
                                <TextBox Text="{Binding Name}" PlaceholderText="Required" IsEnabled="{Binding !IsRunning}" />
                                <TextBlock Classes="hint" Text="The label under the tile on the Wii U Menu; a comma splits it into two lines." Margin="0,1,0,0" />
                            </StackPanel>
                            <StackPanel Spacing="4">
                                <TextBlock Classes="label" Text="Short name" />
                                <TextBox Text="{Binding ShortName}" PlaceholderText="Optional" IsEnabled="{Binding !IsRunning}" />
                                <TextBlock Classes="hint" Text="Used by the HOME Menu and the friend list, not the tile; the tile shows the long name. Defaults to the long name's first line." Margin="0,1,0,0" />
                            </StackPanel>
                            <StackPanel Spacing="4">
                                <TextBlock Classes="label" Text="Product ID" />
                                <TextBox Classes="mono" Text="{Binding ProductId}" MaxLength="4" Width="120" HorizontalAlignment="Left" PlaceholderText="Optional" IsEnabled="{Binding !IsRunning}" />
                                <TextBlock Classes="hint" Text="Optional, 4 characters" Margin="0,1,0,0" />
                            </StackPanel>
                            <CheckBox Content="Use the GamePad as a controller" IsChecked="{Binding GamePad}" IsVisible="{Binding IsGamePadVisible}" IsEnabled="{Binding !IsRunning}" />
                        </StackPanel>
                    </StackPanel>

                    <!-- 3. Artwork -->
                    <StackPanel IsVisible="{Binding IsArtworkStep}">
                        <TextBlock Classes="section" Text="Artwork" />

                        <StackPanel Spacing="16">
                            <!-- Community repository -->
                            <Border Classes="card">
                                <StackPanel Spacing="10">
                                    <TextBlock FontSize="12.5" FontWeight="Bold" Text="Community artwork" />
                                    <TextBlock Classes="hint" FontSize="12" Text="The UWUVCI community keeps icons and boot screens for many games, filed under the game's own code. One click checks whether yours is there." TextWrapping="Wrap" MaxWidth="680" HorizontalAlignment="Left" />
                                    <WrapPanel ItemSpacing="10" VerticalAlignment="Center">
                                        <Button Classes="primary sm" Content="Look up this game" Command="{Binding CommunityArtwork.LookUpCommand}" IsEnabled="{Binding !IsRunning}" />
                                        <TextBlock Classes="hint" FontSize="12" Text="{Binding CommunityArtwork.Status}" VerticalAlignment="Center" TextWrapping="Wrap" MaxWidth="560" />
                                    </WrapPanel>
                                    <StackPanel Orientation="Horizontal" Spacing="16" IsVisible="{Binding CommunityArtwork.HasHit}">
                                        <Button Classes="slot" Command="{Binding ShowImageCommand}" CommandParameter="{Binding CommunityArtwork.Files.Icon}" ToolTip.Tip="Show full size">
                                            <Border Width="96" Height="96" CornerRadius="12" ClipToBounds="True" Background="{DynamicResource SubtleBrush}">
                                                <Image Source="{Binding CommunityArtwork.Files.Icon, Converter={x:Static conv:FileImageConverter.Bitmap}}" Stretch="UniformToFill" />
                                            </Border>
                                        </Button>
                                        <Button Classes="slot" Command="{Binding ShowImageCommand}" CommandParameter="{Binding CommunityArtwork.Files.BootTv}" ToolTip.Tip="Show full size">
                                            <Border Width="256" Height="144" CornerRadius="8" ClipToBounds="True" Background="{DynamicResource SubtleBrush}">
                                                <Image Source="{Binding CommunityArtwork.Files.BootTv, Converter={x:Static conv:FileImageConverter.Bitmap}}" Stretch="UniformToFill" />
                                            </Border>
                                        </Button>
                                        <StackPanel Spacing="8" VerticalAlignment="Center">
                                            <Button Classes="primary sm" Content="Use these" Command="{Binding CommunityArtwork.ApplyCommand}" />
                                            <Button Classes="ghost sm" Content="No thanks" Command="{Binding CommunityArtwork.DismissCommand}" />
                                        </StackPanel>
                                    </StackPanel>
                                </StackPanel>
                            </Border>

                            <!-- Icon -->
                            <Border Classes="card">
                                <Grid ColumnDefinitions="Auto,*" ColumnSpacing="22">
                                    <!-- The slot itself: what will be injected -->
                                    <StackPanel Grid.Column="0" Spacing="7">
                                        <TextBlock Classes="label" Text="{Binding Icon.Label}" />
                                        <Button Classes="slot" Command="{Binding OpenSlotCommand}" CommandParameter="{Binding Icon}" IsEnabled="{Binding !IsRunning}" ToolTip.Tip="Shows the picture full size; pick one when the slot is empty">
                                            <Panel Width="128" Height="128">
                                                <Rectangle Classes="slot-frame" RadiusX="16" RadiusY="16" />
                                                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" IsVisible="{Binding Icon.Path, Converter={x:Static conv:FileImageConverter.IsMissing}}">
                                                    <TextBlock Classes="fine" Text="Menu icon" TextAlignment="Center" />
                                                    <TextBlock Classes="fine" Text="No preview" TextAlignment="Center" IsVisible="{Binding Icon.HasPath}" />
                                                </StackPanel>
                                                <Border CornerRadius="16" ClipToBounds="True" Margin="2">
                                                    <Image Source="{Binding Icon.Path, Converter={x:Static conv:FileImageConverter.Bitmap}}" Stretch="UniformToFill" />
                                                </Border>
                                            </Panel>
                                        </Button>
                                        <StackPanel Orientation="Horizontal" Spacing="8">
                                            <TextBlock Classes="fine" Text="{Binding Icon.Hint}" VerticalAlignment="Center" />
                                            <Button Classes="ghost sm tiny" Content="Pick" Command="{Binding Icon.PickCommand}" IsVisible="{Binding Icon.HasPath}" IsEnabled="{Binding !IsRunning}" />
                                            <Button Classes="ghost sm tiny" Content="Clear" Command="{Binding Icon.ClearCommand}" IsVisible="{Binding Icon.HasPath}" IsEnabled="{Binding !IsRunning}" />
                                        </StackPanel>
                                    </StackPanel>
                                    <!-- Or build one for this slot alone -->
                                    <StackPanel Grid.Column="1" Spacing="10" MaxWidth="520" HorizontalAlignment="Left">
                                        <TextBlock FontSize="12.5" FontWeight="Bold" Text="Build this one" />
                                    <StackPanel Spacing="4" IsVisible="{Binding !ArtworkBuilder.Icon.IsLogo}">
                                        <TextBlock Classes="label" Text="Source image" />
                                        <Grid ColumnDefinitions="*,Auto" ColumnSpacing="8">
                                            <TextBox Grid.Column="0" Text="{Binding ArtworkBuilder.Icon.SourcePath}" FontSize="12" PlaceholderText="Screenshot to put inside the overlay" IsEnabled="{Binding !IsRunning}" />
                                            <Button Grid.Column="1" Classes="sm" Content="Browse…" Command="{Binding ArtworkBuilder.Icon.PickSourceCommand}" IsEnabled="{Binding !IsRunning}" />
                                        </Grid>
                                    </StackPanel>
                                        <StackPanel Spacing="4">
                                            <TextBlock Classes="label" Text="Overlay" />
                                            <ComboBox ItemsSource="{Binding ArtworkBuilder.Icon.Overlays}" SelectedItem="{Binding ArtworkBuilder.Icon.Overlay}" HorizontalAlignment="Stretch" IsEnabled="{Binding !IsRunning}">
                                                <ComboBox.ItemTemplate>
                                                    <DataTemplate x:DataType="vc:ArtworkFrame">
                                                        <StackPanel Orientation="Horizontal" Spacing="10">
                                                            <Border Width="64" Height="36" CornerRadius="4" ClipToBounds="True" Background="{DynamicResource SubtleBrush}">
                                                                <Image Source="{Binding, Converter={x:Static conv:OverlayConverters.Thumbnail}}" Stretch="Uniform" />
                                                            </Border>
                                                            <TextBlock Text="{Binding Name}" VerticalAlignment="Center" />
                                                        </StackPanel>
                                                    </DataTemplate>
                                                </ComboBox.ItemTemplate>
                                            </ComboBox>
                                        </StackPanel>
                                    <Grid ColumnDefinitions="*,*" ColumnSpacing="10" IsVisible="{Binding ArtworkBuilder.Icon.IsBootScreen}">
                                        <StackPanel Grid.Column="0" Spacing="4">
                                            <TextBlock Classes="label" Text="Name, line 1" />
                                            <TextBox Text="{Binding ArtworkBuilder.Icon.NameLine1}" FontSize="12" IsEnabled="{Binding !IsRunning}" />
                                        </StackPanel>
                                        <StackPanel Grid.Column="1" Spacing="4">
                                            <TextBlock Classes="label" Text="Name, line 2" />
                                            <TextBox Text="{Binding ArtworkBuilder.Icon.NameLine2}" FontSize="12" PlaceholderText="Optional" IsEnabled="{Binding !IsRunning}" />
                                        </StackPanel>
                                    </Grid>
                                    <StackPanel Orientation="Horizontal" Spacing="10" IsVisible="{Binding ArtworkBuilder.Icon.IsBootScreen}">
                                        <StackPanel Spacing="4">
                                            <TextBlock Classes="label" Text="Released" />
                                            <TextBox Classes="mono" Text="{Binding ArtworkBuilder.Icon.ReleaseYear}" MaxLength="4" Width="90" FontSize="12" PlaceholderText="Year" IsEnabled="{Binding !IsRunning}" />
                                        </StackPanel>
                                        <StackPanel Spacing="4">
                                            <TextBlock Classes="label" Text="Players" />
                                            <ComboBox ItemsSource="{Binding ArtworkBuilder.Icon.PlayerChoices}" SelectedItem="{Binding ArtworkBuilder.Icon.Players}" Width="90" IsEnabled="{Binding !IsRunning}" />
                                        </StackPanel>
                                    </StackPanel>
                                    <StackPanel Spacing="4" IsVisible="{Binding ArtworkBuilder.Icon.IsLogo}">
                                        <TextBlock Classes="label" Text="Text" />
                                        <TextBox Text="{Binding ArtworkBuilder.Icon.LogoText}" FontSize="12" IsEnabled="{Binding !IsRunning}" />
                                    </StackPanel>
                                        <StackPanel Orientation="Horizontal" Spacing="10">
                                            <Button Classes="primary sm" Content="Build" Command="{Binding ArtworkBuilder.Icon.BuildCommand}" IsEnabled="{Binding !IsRunning}" />
                                            <TextBlock Classes="hint" FontSize="12" Text="Rendering…" VerticalAlignment="Center" IsVisible="{Binding ArtworkBuilder.Icon.IsBuilding}" />
                                        </StackPanel>
                                    </StackPanel>
                                </Grid>
                            </Border>
                            <!-- TV boot screen -->
                            <Border Classes="card">
                                <Grid ColumnDefinitions="Auto,*" ColumnSpacing="22">
                                    <!-- The slot itself: what will be injected -->
                                    <StackPanel Grid.Column="0" Spacing="7">
                                        <TextBlock Classes="label" Text="{Binding BootTv.Label}" />
                                        <Button Classes="slot" Command="{Binding OpenSlotCommand}" CommandParameter="{Binding BootTv}" IsEnabled="{Binding !IsRunning}" ToolTip.Tip="Shows the picture full size; pick one when the slot is empty">
                                            <Panel Width="256" Height="144">
                                                <Rectangle Classes="slot-frame" RadiusX="10" RadiusY="10" />
                                                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" IsVisible="{Binding BootTv.Path, Converter={x:Static conv:FileImageConverter.IsMissing}}">
                                                    <TextBlock Classes="fine" Text="TV boot screen" TextAlignment="Center" />
                                                    <TextBlock Classes="fine" Text="No preview" TextAlignment="Center" IsVisible="{Binding BootTv.HasPath}" />
                                                </StackPanel>
                                                <Border CornerRadius="10" ClipToBounds="True" Margin="2">
                                                    <Image Source="{Binding BootTv.Path, Converter={x:Static conv:FileImageConverter.Bitmap}}" Stretch="UniformToFill" />
                                                </Border>
                                            </Panel>
                                        </Button>
                                        <StackPanel Orientation="Horizontal" Spacing="8">
                                            <TextBlock Classes="fine" Text="{Binding BootTv.Hint}" VerticalAlignment="Center" />
                                            <Button Classes="ghost sm tiny" Content="Pick" Command="{Binding BootTv.PickCommand}" IsVisible="{Binding BootTv.HasPath}" IsEnabled="{Binding !IsRunning}" />
                                            <Button Classes="ghost sm tiny" Content="Clear" Command="{Binding BootTv.ClearCommand}" IsVisible="{Binding BootTv.HasPath}" IsEnabled="{Binding !IsRunning}" />
                                        </StackPanel>
                                    </StackPanel>
                                    <!-- Or build one for this slot alone -->
                                    <StackPanel Grid.Column="1" Spacing="10" MaxWidth="520" HorizontalAlignment="Left">
                                        <TextBlock FontSize="12.5" FontWeight="Bold" Text="Build this one" />
                                    <StackPanel Spacing="4" IsVisible="{Binding !ArtworkBuilder.Tv.IsLogo}">
                                        <TextBlock Classes="label" Text="Source image" />
                                        <Grid ColumnDefinitions="*,Auto" ColumnSpacing="8">
                                            <TextBox Grid.Column="0" Text="{Binding ArtworkBuilder.Tv.SourcePath}" FontSize="12" PlaceholderText="Screenshot to put inside the overlay" IsEnabled="{Binding !IsRunning}" />
                                            <Button Grid.Column="1" Classes="sm" Content="Browse…" Command="{Binding ArtworkBuilder.Tv.PickSourceCommand}" IsEnabled="{Binding !IsRunning}" />
                                        </Grid>
                                    </StackPanel>
                                        <StackPanel Spacing="4">
                                            <TextBlock Classes="label" Text="Overlay" />
                                            <ComboBox ItemsSource="{Binding ArtworkBuilder.Tv.Overlays}" SelectedItem="{Binding ArtworkBuilder.Tv.Overlay}" HorizontalAlignment="Stretch" IsEnabled="{Binding !IsRunning}">
                                                <ComboBox.ItemTemplate>
                                                    <DataTemplate x:DataType="vc:ArtworkFrame">
                                                        <StackPanel Orientation="Horizontal" Spacing="10">
                                                            <Border Width="64" Height="36" CornerRadius="4" ClipToBounds="True" Background="{DynamicResource SubtleBrush}">
                                                                <Image Source="{Binding, Converter={x:Static conv:OverlayConverters.Thumbnail}}" Stretch="Uniform" />
                                                            </Border>
                                                            <TextBlock Text="{Binding Name}" VerticalAlignment="Center" />
                                                        </StackPanel>
                                                    </DataTemplate>
                                                </ComboBox.ItemTemplate>
                                            </ComboBox>
                                        </StackPanel>
                                    <Grid ColumnDefinitions="*,*" ColumnSpacing="10" IsVisible="{Binding ArtworkBuilder.Tv.IsBootScreen}">
                                        <StackPanel Grid.Column="0" Spacing="4">
                                            <TextBlock Classes="label" Text="Name, line 1" />
                                            <TextBox Text="{Binding ArtworkBuilder.Tv.NameLine1}" FontSize="12" IsEnabled="{Binding !IsRunning}" />
                                        </StackPanel>
                                        <StackPanel Grid.Column="1" Spacing="4">
                                            <TextBlock Classes="label" Text="Name, line 2" />
                                            <TextBox Text="{Binding ArtworkBuilder.Tv.NameLine2}" FontSize="12" PlaceholderText="Optional" IsEnabled="{Binding !IsRunning}" />
                                        </StackPanel>
                                    </Grid>
                                    <StackPanel Orientation="Horizontal" Spacing="10" IsVisible="{Binding ArtworkBuilder.Tv.IsBootScreen}">
                                        <StackPanel Spacing="4">
                                            <TextBlock Classes="label" Text="Released" />
                                            <TextBox Classes="mono" Text="{Binding ArtworkBuilder.Tv.ReleaseYear}" MaxLength="4" Width="90" FontSize="12" PlaceholderText="Year" IsEnabled="{Binding !IsRunning}" />
                                        </StackPanel>
                                        <StackPanel Spacing="4">
                                            <TextBlock Classes="label" Text="Players" />
                                            <ComboBox ItemsSource="{Binding ArtworkBuilder.Tv.PlayerChoices}" SelectedItem="{Binding ArtworkBuilder.Tv.Players}" Width="90" IsEnabled="{Binding !IsRunning}" />
                                        </StackPanel>
                                    </StackPanel>
                                    <StackPanel Spacing="4" IsVisible="{Binding ArtworkBuilder.Tv.IsLogo}">
                                        <TextBlock Classes="label" Text="Text" />
                                        <TextBox Text="{Binding ArtworkBuilder.Tv.LogoText}" FontSize="12" IsEnabled="{Binding !IsRunning}" />
                                    </StackPanel>
                                        <StackPanel Orientation="Horizontal" Spacing="10">
                                            <Button Classes="primary sm" Content="Build" Command="{Binding ArtworkBuilder.Tv.BuildCommand}" IsEnabled="{Binding !IsRunning}" />
                                            <TextBlock Classes="hint" FontSize="12" Text="Rendering…" VerticalAlignment="Center" IsVisible="{Binding ArtworkBuilder.Tv.IsBuilding}" />
                                        </StackPanel>
                                    </StackPanel>
                                </Grid>
                            </Border>
                            <!-- GamePad boot screen -->
                            <Border Classes="card">
                                <Grid ColumnDefinitions="Auto,*" ColumnSpacing="22">
                                    <!-- The slot itself: what will be injected -->
                                    <StackPanel Grid.Column="0" Spacing="7">
                                        <TextBlock Classes="label" Text="{Binding BootDrc.Label}" />
                                        <Button Classes="slot" Command="{Binding OpenSlotCommand}" CommandParameter="{Binding BootDrc}" IsEnabled="{Binding !IsRunning}" ToolTip.Tip="Shows the picture full size; pick one when the slot is empty">
                                            <Panel Width="256" Height="144">
                                                <Rectangle Classes="slot-frame" RadiusX="10" RadiusY="10" />
                                                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" IsVisible="{Binding BootDrc.Path, Converter={x:Static conv:FileImageConverter.IsMissing}}">
                                                    <TextBlock Classes="fine" Text="GamePad boot screen" TextAlignment="Center" />
                                                    <TextBlock Classes="fine" Text="No preview" TextAlignment="Center" IsVisible="{Binding BootDrc.HasPath}" />
                                                </StackPanel>
                                                <Border CornerRadius="10" ClipToBounds="True" Margin="2">
                                                    <Image Source="{Binding BootDrc.Path, Converter={x:Static conv:FileImageConverter.Bitmap}}" Stretch="UniformToFill" />
                                                </Border>
                                            </Panel>
                                        </Button>
                                        <StackPanel Orientation="Horizontal" Spacing="8">
                                            <TextBlock Classes="fine" Text="{Binding BootDrc.Hint}" VerticalAlignment="Center" />
                                            <Button Classes="ghost sm tiny" Content="Pick" Command="{Binding BootDrc.PickCommand}" IsVisible="{Binding BootDrc.HasPath}" IsEnabled="{Binding !IsRunning}" />
                                            <Button Classes="ghost sm tiny" Content="Clear" Command="{Binding BootDrc.ClearCommand}" IsVisible="{Binding BootDrc.HasPath}" IsEnabled="{Binding !IsRunning}" />
                                        </StackPanel>
                                    </StackPanel>
                                    <!-- Or build one for this slot alone -->
                                    <StackPanel Grid.Column="1" Spacing="10" MaxWidth="520" HorizontalAlignment="Left">
                                        <TextBlock FontSize="12.5" FontWeight="Bold" Text="Build this one" />
                                    <StackPanel Spacing="4" IsVisible="{Binding !ArtworkBuilder.GamePad.IsLogo}">
                                        <TextBlock Classes="label" Text="Source image" />
                                        <Grid ColumnDefinitions="*,Auto" ColumnSpacing="8">
                                            <TextBox Grid.Column="0" Text="{Binding ArtworkBuilder.GamePad.SourcePath}" FontSize="12" PlaceholderText="Screenshot to put inside the overlay" IsEnabled="{Binding !IsRunning}" />
                                            <Button Grid.Column="1" Classes="sm" Content="Browse…" Command="{Binding ArtworkBuilder.GamePad.PickSourceCommand}" IsEnabled="{Binding !IsRunning}" />
                                        </Grid>
                                    </StackPanel>
                                        <StackPanel Spacing="4">
                                            <TextBlock Classes="label" Text="Overlay" />
                                            <ComboBox ItemsSource="{Binding ArtworkBuilder.GamePad.Overlays}" SelectedItem="{Binding ArtworkBuilder.GamePad.Overlay}" HorizontalAlignment="Stretch" IsEnabled="{Binding !IsRunning}">
                                                <ComboBox.ItemTemplate>
                                                    <DataTemplate x:DataType="vc:ArtworkFrame">
                                                        <StackPanel Orientation="Horizontal" Spacing="10">
                                                            <Border Width="64" Height="36" CornerRadius="4" ClipToBounds="True" Background="{DynamicResource SubtleBrush}">
                                                                <Image Source="{Binding, Converter={x:Static conv:OverlayConverters.Thumbnail}}" Stretch="Uniform" />
                                                            </Border>
                                                            <TextBlock Text="{Binding Name}" VerticalAlignment="Center" />
                                                        </StackPanel>
                                                    </DataTemplate>
                                                </ComboBox.ItemTemplate>
                                            </ComboBox>
                                        </StackPanel>
                                    <Grid ColumnDefinitions="*,*" ColumnSpacing="10" IsVisible="{Binding ArtworkBuilder.GamePad.IsBootScreen}">
                                        <StackPanel Grid.Column="0" Spacing="4">
                                            <TextBlock Classes="label" Text="Name, line 1" />
                                            <TextBox Text="{Binding ArtworkBuilder.GamePad.NameLine1}" FontSize="12" IsEnabled="{Binding !IsRunning}" />
                                        </StackPanel>
                                        <StackPanel Grid.Column="1" Spacing="4">
                                            <TextBlock Classes="label" Text="Name, line 2" />
                                            <TextBox Text="{Binding ArtworkBuilder.GamePad.NameLine2}" FontSize="12" PlaceholderText="Optional" IsEnabled="{Binding !IsRunning}" />
                                        </StackPanel>
                                    </Grid>
                                    <StackPanel Orientation="Horizontal" Spacing="10" IsVisible="{Binding ArtworkBuilder.GamePad.IsBootScreen}">
                                        <StackPanel Spacing="4">
                                            <TextBlock Classes="label" Text="Released" />
                                            <TextBox Classes="mono" Text="{Binding ArtworkBuilder.GamePad.ReleaseYear}" MaxLength="4" Width="90" FontSize="12" PlaceholderText="Year" IsEnabled="{Binding !IsRunning}" />
                                        </StackPanel>
                                        <StackPanel Spacing="4">
                                            <TextBlock Classes="label" Text="Players" />
                                            <ComboBox ItemsSource="{Binding ArtworkBuilder.GamePad.PlayerChoices}" SelectedItem="{Binding ArtworkBuilder.GamePad.Players}" Width="90" IsEnabled="{Binding !IsRunning}" />
                                        </StackPanel>
                                    </StackPanel>
                                    <StackPanel Spacing="4" IsVisible="{Binding ArtworkBuilder.GamePad.IsLogo}">
                                        <TextBlock Classes="label" Text="Text" />
                                        <TextBox Text="{Binding ArtworkBuilder.GamePad.LogoText}" FontSize="12" IsEnabled="{Binding !IsRunning}" />
                                    </StackPanel>
                                        <StackPanel Orientation="Horizontal" Spacing="10">
                                            <Button Classes="primary sm" Content="Build" Command="{Binding ArtworkBuilder.GamePad.BuildCommand}" IsEnabled="{Binding !IsRunning}" />
                                            <TextBlock Classes="hint" FontSize="12" Text="Rendering…" VerticalAlignment="Center" IsVisible="{Binding ArtworkBuilder.GamePad.IsBuilding}" />
                                        </StackPanel>
                                    </StackPanel>
                                </Grid>
                            </Border>
                            <!-- Boot logo -->
                            <Border Classes="card">
                                <Grid ColumnDefinitions="Auto,*" ColumnSpacing="22">
                                    <!-- The slot itself: what will be injected -->
                                    <StackPanel Grid.Column="0" Spacing="7">
                                        <TextBlock Classes="label" Text="{Binding BootLogo.Label}" />
                                        <Button Classes="slot" Command="{Binding OpenSlotCommand}" CommandParameter="{Binding BootLogo}" IsEnabled="{Binding !IsRunning}" ToolTip.Tip="Shows the picture full size; pick one when the slot is empty">
                                            <Panel Width="227" Height="56">
                                                <Rectangle Classes="slot-frame" RadiusX="8" RadiusY="8" />
                                                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" IsVisible="{Binding BootLogo.Path, Converter={x:Static conv:FileImageConverter.IsMissing}}">
                                                    <TextBlock Classes="fine" Text="Boot logo" TextAlignment="Center" />
                                                    <TextBlock Classes="fine" Text="No preview" TextAlignment="Center" IsVisible="{Binding BootLogo.HasPath}" />
                                                </StackPanel>
                                                <Border CornerRadius="8" ClipToBounds="True" Margin="2">
                                                    <Image Source="{Binding BootLogo.Path, Converter={x:Static conv:FileImageConverter.Bitmap}}" Stretch="Uniform" />
                                                </Border>
                                            </Panel>
                                        </Button>
                                        <StackPanel Orientation="Horizontal" Spacing="8">
                                            <TextBlock Classes="fine" Text="{Binding BootLogo.Hint}" VerticalAlignment="Center" />
                                            <Button Classes="ghost sm tiny" Content="Pick" Command="{Binding BootLogo.PickCommand}" IsVisible="{Binding BootLogo.HasPath}" IsEnabled="{Binding !IsRunning}" />
                                            <Button Classes="ghost sm tiny" Content="Clear" Command="{Binding BootLogo.ClearCommand}" IsVisible="{Binding BootLogo.HasPath}" IsEnabled="{Binding !IsRunning}" />
                                        </StackPanel>
                                    </StackPanel>
                                    <!-- Or build one for this slot alone -->
                                    <StackPanel Grid.Column="1" Spacing="10" MaxWidth="520" HorizontalAlignment="Left">
                                        <TextBlock FontSize="12.5" FontWeight="Bold" Text="Build this one" />
                                    <StackPanel Spacing="4" IsVisible="{Binding !ArtworkBuilder.Logo.IsLogo}">
                                        <TextBlock Classes="label" Text="Source image" />
                                        <Grid ColumnDefinitions="*,Auto" ColumnSpacing="8">
                                            <TextBox Grid.Column="0" Text="{Binding ArtworkBuilder.Logo.SourcePath}" FontSize="12" PlaceholderText="Screenshot to put inside the overlay" IsEnabled="{Binding !IsRunning}" />
                                            <Button Grid.Column="1" Classes="sm" Content="Browse…" Command="{Binding ArtworkBuilder.Logo.PickSourceCommand}" IsEnabled="{Binding !IsRunning}" />
                                        </Grid>
                                    </StackPanel>
                                        <StackPanel Spacing="4">
                                            <TextBlock Classes="label" Text="Overlay" />
                                            <ComboBox ItemsSource="{Binding ArtworkBuilder.Logo.Overlays}" SelectedItem="{Binding ArtworkBuilder.Logo.Overlay}" HorizontalAlignment="Stretch" IsEnabled="{Binding !IsRunning}">
                                                <ComboBox.ItemTemplate>
                                                    <DataTemplate x:DataType="vc:ArtworkFrame">
                                                        <StackPanel Orientation="Horizontal" Spacing="10">
                                                            <Border Width="64" Height="36" CornerRadius="4" ClipToBounds="True" Background="{DynamicResource SubtleBrush}">
                                                                <Image Source="{Binding, Converter={x:Static conv:OverlayConverters.Thumbnail}}" Stretch="Uniform" />
                                                            </Border>
                                                            <TextBlock Text="{Binding Name}" VerticalAlignment="Center" />
                                                        </StackPanel>
                                                    </DataTemplate>
                                                </ComboBox.ItemTemplate>
                                            </ComboBox>
                                        </StackPanel>
                                    <Grid ColumnDefinitions="*,*" ColumnSpacing="10" IsVisible="{Binding ArtworkBuilder.Logo.IsBootScreen}">
                                        <StackPanel Grid.Column="0" Spacing="4">
                                            <TextBlock Classes="label" Text="Name, line 1" />
                                            <TextBox Text="{Binding ArtworkBuilder.Logo.NameLine1}" FontSize="12" IsEnabled="{Binding !IsRunning}" />
                                        </StackPanel>
                                        <StackPanel Grid.Column="1" Spacing="4">
                                            <TextBlock Classes="label" Text="Name, line 2" />
                                            <TextBox Text="{Binding ArtworkBuilder.Logo.NameLine2}" FontSize="12" PlaceholderText="Optional" IsEnabled="{Binding !IsRunning}" />
                                        </StackPanel>
                                    </Grid>
                                    <StackPanel Orientation="Horizontal" Spacing="10" IsVisible="{Binding ArtworkBuilder.Logo.IsBootScreen}">
                                        <StackPanel Spacing="4">
                                            <TextBlock Classes="label" Text="Released" />
                                            <TextBox Classes="mono" Text="{Binding ArtworkBuilder.Logo.ReleaseYear}" MaxLength="4" Width="90" FontSize="12" PlaceholderText="Year" IsEnabled="{Binding !IsRunning}" />
                                        </StackPanel>
                                        <StackPanel Spacing="4">
                                            <TextBlock Classes="label" Text="Players" />
                                            <ComboBox ItemsSource="{Binding ArtworkBuilder.Logo.PlayerChoices}" SelectedItem="{Binding ArtworkBuilder.Logo.Players}" Width="90" IsEnabled="{Binding !IsRunning}" />
                                        </StackPanel>
                                    </StackPanel>
                                    <StackPanel Spacing="4" IsVisible="{Binding ArtworkBuilder.Logo.IsLogo}">
                                        <TextBlock Classes="label" Text="Text" />
                                        <TextBox Text="{Binding ArtworkBuilder.Logo.LogoText}" FontSize="12" IsEnabled="{Binding !IsRunning}" />
                                    </StackPanel>
                                        <StackPanel Orientation="Horizontal" Spacing="10">
                                            <Button Classes="primary sm" Content="Build" Command="{Binding ArtworkBuilder.Logo.BuildCommand}" IsEnabled="{Binding !IsRunning}" />
                                            <TextBlock Classes="hint" FontSize="12" Text="Rendering…" VerticalAlignment="Center" IsVisible="{Binding ArtworkBuilder.Logo.IsBuilding}" />
                                        </StackPanel>
                                    </StackPanel>
                                </Grid>
                            </Border>
                        </StackPanel>

                        <!-- Boot sound -->
                        <Border Classes="divider" Margin="0,18,0,0" Padding="0,16,0,0" MaxWidth="700" HorizontalAlignment="Left">
                            <Grid ColumnDefinitions="*,Auto,Auto,Auto" ColumnSpacing="10">
                                <StackPanel Grid.Column="0" Spacing="4">
                                    <TextBlock Classes="label" Text="{Binding BootSound.Label}" />
                                    <TextBox Text="{Binding BootSound.Path}" IsReadOnly="True" PlaceholderText="Optional" />
                                </StackPanel>
                                <Button Grid.Column="1" Classes="sm icon" Command="{Binding PreviewSoundCommand}" VerticalAlignment="Bottom" ToolTip.Tip="Play it as the console will">
                                    <Panel>
                                        <Viewbox Width="14" Height="14" IsVisible="{Binding !IsPlayingSound}">
                                            <Canvas Width="24" Height="24">
                                                <Path Classes="btn-glyph" Fill="{DynamicResource ButtonTextBrush}" Data="M7 4.5v15l12-7.5Z" />
                                            </Canvas>
                                        </Viewbox>
                                        <Viewbox Width="14" Height="14" IsVisible="{Binding IsPlayingSound}">
                                            <Canvas Width="24" Height="24">
                                                <Path Classes="btn-glyph" Fill="{DynamicResource ButtonTextBrush}" Data="M6 6h12v12H6Z" />
                                            </Canvas>
                                        </Viewbox>
                                    </Panel>
                                </Button>
                                <Button Grid.Column="2" Classes="sm" Content="Pick" Command="{Binding BootSound.PickCommand}" IsEnabled="{Binding !IsRunning}" VerticalAlignment="Bottom" />
                                <Button Grid.Column="3" Classes="ghost sm" Content="Clear" Command="{Binding BootSound.ClearCommand}" IsEnabled="{Binding !IsRunning}" VerticalAlignment="Bottom" />
                            </Grid>
                        </Border>
                        <TextBlock Classes="fine" Text="{Binding BootSound.Hint}" Margin="0,6,0,0" />
                    </StackPanel>

                    <!-- 4. Options -->
                    <StackPanel IsVisible="{Binding IsOptionsStep}">
                        <TextBlock Classes="section" Text="{Binding SelectedConsoleName, StringFormat='Options · {0}'}" />
                        <ContentControl Content="{Binding CurrentOptions}" IsEnabled="{Binding !IsRunning}" />
                    </StackPanel>

                    <!-- 5. Review & Inject -->
                    <StackPanel IsVisible="{Binding IsReviewStep}">
                        <TextBlock Classes="section" Text="Review &amp; Inject" />
                        <StackPanel Spacing="10" Margin="0,0,0,16">
                            <StackPanel Orientation="Horizontal" Spacing="10">
                                <TextBlock Classes="summary-label" Text="Console" />
                                <TextBlock Classes="summary-value" Text="{Binding SelectedConsoleName}" />
                            </StackPanel>
                            <StackPanel Orientation="Horizontal" Spacing="10">
                                <TextBlock Classes="summary-label" Text="{Binding ReviewTemplateLabel}" />
                                <TextBlock Classes="summary-value" Text="{Binding ReviewTemplate}" />
                            </StackPanel>
                            <StackPanel Orientation="Horizontal" Spacing="10">
                                <TextBlock Classes="summary-label" Text="ROM" />
                                <TextBlock Classes="summary-value" Text="{Binding ReviewRom}" />
                            </StackPanel>
                            <StackPanel Orientation="Horizontal" Spacing="10">
                                <TextBlock Classes="summary-label" Text="Game" />
                                <TextBlock Classes="summary-value" Text="{Binding ReviewGame}" />
                            </StackPanel>
                            <StackPanel Orientation="Horizontal" Spacing="10">
                                <TextBlock Classes="summary-label" Text="Output" VerticalAlignment="Center" />
                                <ComboBox ItemsSource="{Binding Formats}" SelectedItem="{Binding Format}" Width="130" IsEnabled="{Binding !IsRunning}">
                                    <ComboBox.ItemTemplate>
                                        <DataTemplate x:DataType="vc:OutputFormat">
                                            <TextBlock Text="{Binding, Converter={x:Static conv:FormatConverters.Name}}" />
                                        </DataTemplate>
                                    </ComboBox.ItemTemplate>
                                </ComboBox>
                                <TextBlock Classes="hint" FontSize="12" Text="{Binding ReviewOutput}" VerticalAlignment="Center" />
                            </StackPanel>
                        </StackPanel>

                        <Border Classes="banner-warning" Margin="0,0,0,14" IsVisible="{Binding HasMissingKeys}">
                            <TextBlock Text="{Binding MissingKeysHint}" />
                        </Border>
                        <Border Classes="banner-warning" Margin="0,0,0,14" IsVisible="{Binding HasRomFitHint}">
                            <TextBlock Text="{Binding RomFitHint}" />
                        </Border>
                        <StackPanel Spacing="4" Margin="0,0,0,14" IsVisible="{Binding HasReviewExtras}">
                            <TextBlock Classes="label" Text="Also to the SD card" />
                            <ItemsControl ItemsSource="{Binding ReviewExtras}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate x:DataType="x:String">
                                        <TextBlock Classes="summary-value" Text="{Binding}" />
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                            <TextBlock Classes="hint" FontSize="12" Text="Copied when the title is copied to the card; with that setting off, copy them yourself." />
                        </StackPanel>
                        <ItemsControl ItemsSource="{Binding AromaWarnings}" IsVisible="{Binding HasAromaWarnings}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate x:DataType="x:String">
                                    <Border Classes="banner-warning" Margin="0,0,0,14">
                                        <TextBlock Text="{Binding}" />
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                        <TextBlock Classes="hint" FontSize="12" Text="{Binding RetroArchHint}" TextWrapping="Wrap" Margin="0,0,0,14" IsVisible="{Binding IsRetroArch}" />

                        <Border Classes="divider" Padding="0,16,0,0" Margin="0,4,0,0">
                            <StackPanel Orientation="Horizontal" Spacing="10" HorizontalAlignment="Center">
                                <Button Classes="primary lg" Content="Inject" Command="{Binding InjectCommand}" />
                                <Button Content="Cancel" Command="{Binding CancelCommand}" VerticalAlignment="Center" />
                                <TextBlock Text="{Binding Status}" FontSize="13" FontWeight="SemiBold" VerticalAlignment="Center" />
                                <TextBlock Text="{Binding OutputSize, Converter={x:Static conv:SizeConverters.Text}}" Foreground="{Binding OutputSize, Converter={x:Static conv:SizeConverters.Brush}}" FontSize="13" FontWeight="Bold" VerticalAlignment="Center" IsVisible="{Binding HasOutputSize}" ToolTip.Tip="The finished title on disk" />
                                <TextBlock Text="{Binding CurrentStep}" FontSize="12" Foreground="{DynamicResource TextTertiaryBrush}" VerticalAlignment="Center" />
                            </StackPanel>
                        </Border>

                        <ProgressBar IsIndeterminate="True" IsVisible="{Binding IsRunning}" Margin="0,14,0,0" Height="6" MinHeight="6" CornerRadius="4"
                                     Foreground="{DynamicResource CyanBrush}" Background="{DynamicResource DividerBrush}" />

                        <Border Classes="log" Margin="0,14,0,0" IsVisible="{Binding !!Log.Count}">
                            <ScrollViewer behaviors:AutoScroll.ToEnd="True">
                                <ItemsControl ItemsSource="{Binding Log}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate x:DataType="x:String">
                                            <TextBlock Text="{Binding}" />
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </ScrollViewer>
                        </Border>
                    </StackPanel>
                </Panel>
            </Border>
        </StackPanel>

    </Grid>
</UserControl>
